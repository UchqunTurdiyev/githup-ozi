# githup-ozi

Next.js (App Router) loyihasi — brauzerda **Hello World** so'zini chiqaradi.

## Ishga tushirish

```bash
npm install
npm run dev
```

So'ng brauzerda [http://localhost:3000](http://localhost:3000) manzilini oching.
